.env
.venv/
__pycache__/
*.db

# 通話録音(個人データ。絶対にコミットしない)
recordings/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
.terraform.tfstate.lock.info
crash.log

# Docker
*.local.yml
tfplan
infra/.build/
node_modules/
frontend/dist/
# macOS(iCloud同期)が作る衝突コピー。マイグレーションに紛れ込むと
# alembicのheadが割れて起動不能になる(2026-08-03に実際に発生)
* 2.*
*.tsbuildinfo
